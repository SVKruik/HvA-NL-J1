import MainMenu from "./components/MainMenu";

const Topbar = () => {
    return <header className="topbar">
        <nav>
            <MainMenu />
        </nav>
    </header>;
}

export default Topbar;