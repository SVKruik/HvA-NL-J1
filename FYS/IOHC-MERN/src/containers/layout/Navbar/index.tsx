import MainMenu from "./components/MainMenu";

const Navbar = () => {
    return <header className="Navbar">
        <nav>
            <MainMenu />
        </nav>
    </header>;
}

export default Navbar;